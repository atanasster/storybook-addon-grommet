import { FunctionComponent } from 'react';
import { Channel } from '@storybook/channels';
interface ThemeSelectorProps {
    channel: Channel;
}
export declare const ThemeSelector: FunctionComponent<ThemeSelectorProps>;
export {};
